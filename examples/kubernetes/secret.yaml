---
# Only for devices bound to a TP-Link account. Do not commit real values.
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: kasa-credentials
stringData:
  KASA_USERNAME: <tp-link-account-email>
  KASA_PASSWORD: <tp-link-account-password>
